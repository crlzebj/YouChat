server:
  port: 8080

spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/you_chat
      username: root
      password: '030514'

mybatis:
  # mapper配置文件
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.zjx.youchat.pojo.entity
  configuration:
    # 开启驼峰命名
    map-underscore-to-camel-case: true
